/// <reference types="vite/client" />

// Build-time constants injected by `define` in vite.config.ts. Available
// throughout the app at runtime as ordinary global identifiers.
declare const __BUILD_TIME__: string;
